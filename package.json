{
  "name": "snakewolf-games-launcher",
  "version": "1.0.0",
  "description": "Epic Games Launcher inspired by SnakeWolf Games",
  "main": "electron.js",
  "scripts": {
    "start": "electron .",
    "dev:react": "npm run dev --prefix client",
    "build:react": "npm run build --prefix client",
    "build:electron": "electron-builder",
    "build": "npm run build:react && npm run build:electron"
  },
  "devDependencies": {
    "electron": "^36.3.1",
    "electron-builder": "^26.0.12",
    "electron-vite": "^3.1.0"
  }
}
